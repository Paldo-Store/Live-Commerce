spring:
  config:
    import: "configserver:http://localhost:18080"
  application:
    name: coupon
  profiles:
    active: dev

#spring:
#  profiles:
#    active: prod
#  cloud:
#    config:
#      enabled: false