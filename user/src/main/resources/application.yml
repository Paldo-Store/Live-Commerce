spring:
  config:
    import: "configserver:http://localhost:18080"
  application:
    name: user
  profiles:
    active: dev

#spring:
#  profiles:
#    active: prod
#  cloud:
#    config:
#      enabled: false